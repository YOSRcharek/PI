{% extends 'base.back.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ Service.id }}</td>
            </tr>
            <tr>
                <th>NomService</th>
                <td>{{ Service.nomService }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ Service.description }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ Service.disponibilite ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

      <a href="{{ path('app_Service_back_index') }}" style="background-color: brown; color: white; padding: 10px; border-radius: 5px; text-decoration: none; width: 150px; display: inline-block;"><i class="fa fa-arrow-left"></i> back to list</a>

    
     <a href="{{ path('app_Service_edit', {'id': Service.id}) }}" style="background-color: blue; color: white; padding: 10px; border-radius: 5px; text-decoration: none; width: 100px; display: inline-block;"><i class="fa fa-edit"></i> edit</a>
    {{ include('back/Service/_delete_form.html.twig') }}
{% endblock %}
