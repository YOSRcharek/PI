{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content comment</th>
                <td>{{ comment.contentcomment }}</td>
            </tr>
            <tr>
                <th>Upvotes</th>
                <td>{{ comment.upvotes }}</td>
            </tr>
            <tr>
                <th>Create date comment</th>
                <td>{{ comment.createdatcomment ? comment.createdatcomment|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_index') }}">back to list</a>

    <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">edit</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
